apiVersion: apps/v1
metadata:
   name: catalog-manager-server
   namespace: catalog-ns
kind: Deployment
spec:
    replicas: 1
    selector:
      matchLabels:
          app: catalog-manager-server
    template:
       metadata:
           labels:
            app: catalog-manager-server
       spec:
        serviceAccountName: catalog-server-sa
        containers:
        - name: cnt-1
          

          image:  us-docker.pkg.dev/vikraya-ecommerce/vikraya-ecommerce/catalog-service
          ports:
          - containerPort: 8080
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi